__pycache__/
*.pyc
*.pyo
.pytest_cache/
logs/
# Biểu đồ kết quả (results/*.png) ĐƯỢC commit để báo cáo hiển thị hình; chỉ bỏ qua CSV thô.
results/*.csv
.vscode/
.idea/

# Script kiểm tra winrate cục bộ (không track) — bỏ qua mọi thứ trong experiments/
experiments/*
# ...NHƯNG vẫn commit bản weights CFR đã nén (~7MB) để clone về chạy CFR mạnh ngay:
!experiments/cfr_heavy.weights.json.gz
# Bản weights chưa nén (~51MB) thì không bao giờ track:
*.weights.json
